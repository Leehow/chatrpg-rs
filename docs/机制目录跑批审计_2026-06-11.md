# 机制目录跑批审计（Slice A 验收闸门 / 计划任务 A7）

> 日期：2026-06-11（实跑日）。范围：六套规则（CoC 7e / Cyberpunk RED / Triangle Agency / BRP-ORC / D&D 5e 中文 / 剑世界 2.5 中文）真库真 LLM 跑批 + spec 附录 A 137 条覆盖率审计。
> 跑批工具：`mechanics_proto`（gpt-5.4 经 relay :18888，budget=14，六套串行，`TRPG_LLM_TIMEOUT_SECS=600`）。
> 输入：六套 **live kernel dump**（:54347 `rule_kernels.content_json`，跑批前 `mechanics_catalog` 全部为 0）+ semantic units + markdown sidecar。
> 落库：五套（CPR/Triangle/ORC/D&D/SW）proto `--out` 产物经纯数据 SQL 写回 :54347；CoC 走完整 parse 重抽验证 A6 接线全链（管线内第三遍 → `upsert_rule_kernel`）。
> 产物目录：`/Users/haoli/leehow/code/chatrpgv2/_a7_mech_audit/`（`{ruleset}.upgraded.json` 升级后 kernel、`{ruleset}.audit.jsonl` 审计 dump、`{ruleset}.log` 编译日志；CoC 含 `.v1/.v2` 迭代存档）。
> 附录 A 为 7 书 137 条；**Fate 圣杯战争（A11，18 条）无已编译 kernel/units，批外记录不计分母**（spec 附录 A 自身标注其为补勘追加）。

## 0. 断言矩阵总览（详证见各节）

| # | 断言 | 结果 |
|---|---|---|
| 1 | 六套 mechanics_catalog 非空生成；validation_report 记录每次丢弃及原因 | ✅ §7/§4 |
| 2 | CoC 含 sanity_check / temporary_insanity 语义条目；followup 链通；跳跃条目带 when_to_use | ✅ §2/§7（DB 终态） |
| 3a | 附录 A 覆盖率审计：逐套缺失清单 + 三抽查（Other 类 / 纯语义 / EngineHook） | ✅ §2-§5/§9 |
| 12① | CoC success_bands 含 critical 与 failure 侧 band + `success_band_completed` 证据 | ✅ §2/§7（DB 终态） |
| 工程三查 | 新文件 ≤400 行 / 零规则集字面量 / serde(default) 回归 | ✅ §6 |

## 1. 跑批操作记录（含教训与 prompt 迭代史）

- 工具链：`mechanics_proto <units> <kernel-dump> <sidecar> 14 --out … --audit …`；`--missing`（离线）把审计缺失清单以 `catalog_coverage_gap` 写进 kernel `validation_report`（机器可见半边）。
- **教训 1（前次中断会话遗产，本次复用）**：`trpg-llm` 默认 `timeout_secs=180`，gpt-5.4 单轮长生成 >3 分钟 → HTTP 超时 → transport error 重试空转。跑批环境必须 `TRPG_LLM_TIMEOUT_SECS=600`。本会话首跑 CoC 没带该变量被及时杀掉重启。
- **教训 2（输入选择）**：proto 输入 kernel 用 **live DB dump** 而非 `_rstest_*/parsed/rules/` artifact——落库走同一份数据，且 round-trip 验证过 RuleKernel 反序列化只增 `mechanics_catalog` 键、原 key 全保留（serde(default) 向后兼容的实证）。
- **Prompt 迭代史（A2 `MECH_SYS`，全部通用措辞、零规则集字面量）**：
  - **v1（A2 原版）**：CoC 48 条全 procedure 档——0 hook 档/0 纯语义、无 jump、critical band 未补、warnings=0。系统性缺陷三处。
  - **v2（迭代①）**：WHAT QUALIFIES 增"社会/关系常驻机制、信息经济、元资源也是 play-time 机制（纯 prose 也收）"+ per-skill 触发语义入册指引；tier-4 增"禁止给 prose 机制编造步骤、健康目录四档俱全"；band sweep 增"必须扫全成功度阶梯到 plain failure"。CoC 52 条：开放 kind 涌现（social_status_rule/backstory_rewrite/information_rule…）、failure band 补全留证；但 critical 仍漏、jump 仍漏（模型按 LEGAL KEYS 自选了 9 个技能）。
  - **v3（迭代②）**：band 行增"EXACT die result 档（最小/最大骰）也是 band"；技能行增"优先收冒险叙事常用的位移/攀爬/游泳/潜行/侦察/社交施压类"。CoC 46 条：**critical+failure 双补全留证、skill_jump 带真 when_to_use**——闸门级缺口清零。
  - **v4（迭代③，五套与 CoC parse 重抽用此版）**：v3 产物暴露新 band 缺机器可读 `test{}` 谓词（contest `band_test_passes` 无 `test` 即永不匹配 → display-only）。band 行增"必须镜像既有 band 的 test 谓词形状 + rank"。
- 串行纪律全程遵守（同一时刻只有一个 proto 进程）；每套 6-8 分钟（CoC 1648 units 约 360-382s）。

## 2. call_of_cthulhu_7e（v3 prompt 产物；DB 落库走 parse 重抽另验）

- 条目数 **46**；kind 分布：subsystem_procedure 22 / skill_check 9 / gm_judgement_rule 3 / information_rule 3 / reaction 3 / 开放新 kind 7 类（modifier_rule, standing_rule, standing_effect, upkeep_procedure, resource_change, spend）；tier：procedure 38 / passive_modifier 8。
- **验收 2 证据**：
  - sanity check 类：`sanity_roll_and_loss`（"投 SAN、失败掉san"语义）✅；temporary insanity 类：`sanity_temporary_insanity_check`（when_to_use="Use this immediately after a single event causes the investigator to lose five or more…"）✅——均按 when_to_use/name 语义认定，非 id 字面。
  - followup 链通：`sanity.thresholds[loss_in_one_go:5].followup_procedure_id = call_of_cthulhu_7e.sanity_temporary_insanity_check`，该 id 在目录内存在 ✅；`at:0 → sanity_permanent_break` 同样闭合 ✅。
  - 跳跃条目：`skill_jump`，when_to_use="Call for this when an investigator leaps a gap, jumps down from a height, attempts an unusually long or difficult jump…" ✅（tested_parameter=null——见 §8 遗留 ①）。
- **验收 12① 证据**（proto 摘要原文）：
  ```
  success_band_completed [failure]: kernel lacked this success band; whole object appended from the compile pass
  success_band_completed [critical]: kernel lacked this success band; whole object appended from the compile pass
  ```
  升级后 bands = regular/hard/extreme/fumble/failure/critical（critical 带 semantics="A roll of 01…maximum damage"）。书证：sidecar L6743 "01: A Critical Success"、L6886 "Critical success: a roll of 01"。
- **3a 三抽查**：非预设 kind ✅（7 个开放类，如 `gm_judgement_rule`/`standing_effect`）；纯语义条目（spec 判据 procedure 空）✅（8 个 PM 档，如 `magic_general_costs_and_risks`、`skill_climb`）；EngineHook ✅（6 条：combat_start / session_end / rest / **calendar 月度×2**——疯狂治疗月度结算被数据化为 `{"event":"calendar","granularity":{"unit":"month","seconds_per_unit":2592000}}`）。
- **覆盖率缺失清单**（对照附录 A CoC 14 行基线，≈10/14 全中 + 部分覆盖；缺失经 `--missing` 写回落库 kernel）：
  1. Credit Rating 信用评级常驻社会场（A1；v2 run 曾抽到——run 间漂移实证）
  2. Investigator Development Phase 剧本末结算（A5；v1/v2 曾抽到）
  3. Contacts 人脉（A7；三 run 均未抽到）
  4. Mythos 侵蚀上限（99-CM）/习惯恐怖 per-怪计数/Believer 延迟债（A2 部分覆盖）
  5. 法术副作用表（A9 部分覆盖）

## 3. cyberpunk_red（v4 prompt；第二次 run——首跑 132s 空产出 fail-closed 留原样，重跑成功）

- 条目数 **42**（procedure 33 / passive_modifier 9）；wall-clock 328s。**首跑教训**：两轮 ReAct 均未 submit（132s 异常短，疑似 relay 瞬断或模型未走到提交），fail-closed 正确生效——kernel 原样 + gap note "mechanics compiler produced nothing"，零污染；重跑即恢复。
- **band 补全**：`success_band_completed` ×2（critical_failure + failure）——meet_or_beat 系的失败侧档位由编译遍补齐。
- **3a 三抽查**：非预设 kind ✅（turn_structure / recurring_pressure / check_modifier / outcome_resolution / resource_interpretation / social_standing / downtime_procedure / followup_rule / status_track / opposed_check / check_substitute / recurring_check——12 个开放类）；EngineHook ✅（9 条：development_phase ×4、calendar、turn_start ×5——humanity 治疗挂 development_phase、hustle 挂 calendar+development_phase）；纯语义 ✅（9 个 PM 档，如 addiction_roleplay_pressure）。
- 亮点：Netrunning 子系统整链（jack_in/cloak/control_node/backdoor/scanner）、wound_states（status_track）+ mortally_wounded（followup_rule）、humanity_emp_band（resource_interpretation——EMP 联动语义）。
- **缺失清单**（8 条经 --missing 落库；附录 A CPR 11 行基线 ≈6/11 全中）：LUCK Pool（元资源重要缺口）、Operator/Moto/Teamwork/粉丝感召/Media Credibility（Roles 章能力层系统性未深扫）、Media 每周谣言流、月初 Lifestyle/Trauma Team、Trying Again、Night Market。

## 4. triangle_agency（v4 prompt）

- 条目数 **25**（procedure 19 / passive_modifier 5 / hook 1）；wall-clock 282s。
- **band 补全**：`success_band_completed` ×3——**triscendence / success / failure**（count_faces 系的成功度阶梯被建出来，且带 test+rank——v4 的谓词形状要求生效）。
- **validation_report 丢弃留证**（断言 1 的"每次丢弃及原因"实证）：`followup_link_broken [triangle_agency.core_roll]: followup link points at triangle_agency.burnout_from_other_sources which is not in the final catalog`。
- **3a 三抽查**：非预设 kind ✅（standing_field / policy_trigger / fictional_positioning / outcome_degree / gm_resource / development_choice / scene_structure / standing_boon / standing_modifier）；EngineHook ✅（morning_meetings_scene_use=hook 档）；纯语义 ✅（5 PM：commendations_and_demerits、loose_ends、capture_anomalies_by_focus 等）。
- 亮点：**Playwalled 实证落 `locked_until`**（playwalled_document_access.locked_until="Specific entries remain locked until directed by another rule or choice."——spec A10 ③ 条件解锁条目设计的真实落地）；嘉奖/记过（standing_field PM）+ Prime Directive（policy_trigger）= 行为语义监听双件套入册；chaos_pool 含 GM 花池语义；Work/Life Balance 三轨（competency/reality time spend + time_phase）。
- **缺失清单**（4 条落库；A10 15 行基线 ≈11/15 全中）：Conflict Resolution PvP、Reality Trigger+崩坏 track、Harm/Life Insurance、Weather Events 阶梯。

## 5. brp_orc / dnd5e / sword_world_2_5（v4 prompt）

**brp_orc：38 条**（procedure 25 / PM 7 / hook 6；wall-clock 275s）。band 补全 failure ×1（bands=critical/special/regular/fumble/failure 全带 test）。三抽查：开放 kind ✅（recurring_hazard / development_flag / development_phase / resource_recovery / social_standing / standing_procedure）；hook ✅（6 条：ambush、cold_exposure_hunger_thirst、fatigue_recovery、natural_healing、power_point_recovery、time_and_movement）；纯语义 ✅（7 PM 含 jump/persuade/spot/track——per-skill 触发语义指引跨规则集生效）。缺失 7 条落库（Augment、Passions、Allegiance、衰老、Dying Blows、POW 牺牲、Status/Wealth 档——书内命中数均已核实非幻觉缺口）。

**dnd5e：65 条**（procedure 30 / PM 35；wall-clock 338s）。band 补全 success/failure（d20 meet_or_beat 原 kernel 无 bands）。三抽查：开放 kind ✅（background_feature / scene_state / outcome_interpretation / permission_rule / resource_state / social_state / passive_score / recurring_upkeep）；hook ✅（10 条——combat_sequence=combat_start+turn_start、food_and_water=time_advance+**calendar 日粒度数据化**、forced_march=**calendar 时粒度**、long_rest=rest+calendar、death_saving_throw=turn_start）；纯语义 ✅（35 PM——15 个状态 conditions 全量入册成 scene_state）。亮点：背景特性三条（background_feature kind——附录 A1 实证）、被动检定（passive_score）、jump_high/jump_long。缺失 6 条落库（优势/劣势元规则、四件套、狂野魔法浪涌、神圣干预、生活方式、击晕选项——书内命中数已核实）。

**sword_world_2_5：42 条**（procedure 29 / PM 13；wall-clock 333s）。三抽查：开放 kind ✅（gm_initiated_check / timed_effect_procedure）；hook ✅（8 条：combat_start ×5、combat_end、turn_start、calendar）；纯语义 ✅（13 PM 含 jump_check）。亮点：**GM 指令型暗骰三连全中**（danger_sense / detect / notice / spot_trap 全部 gm_initiated_check kind——附录 A8 实证）+ monster_knowledge_check。缺失 9 条落库（名誉点数、Disposition、Soulscars、Sword's Grace、Exceptional+双1、每日结算/session 三岔、Fellows、浅渊/欠片、复活仪式——Core I 内命中数均核实；本套缺口最多，战役/社会层章节未被扫到，combat/check 层很全）。

## 6. 工程三查（断言 5）

- **≤400 行**（`wc -l`）：mechanics.rs 399 / mechanics_render.rs 161 / mechanics_compile.rs 373 / mechanics_compile_tests.rs 190 / mechanics_finalize.rs 248 / mechanics_finalize_tests.rs 187 / mechanics_upgrade_tests.rs 205 / mechanics_upgrades.rs 224 / mechanics_proto.rs 243 ✅
- **零规则集字面量**：`grep -rn -i "cthulhu\|cyberpunk\|triangle\|sword.world\|剑世界\|龙与地下城"` 对 trpg-model/src/mechanics.rs + trpg-rule-agent/src/reader/mechanics_*.rs + bin/mechanics_proto.rs **零命中**（连注释/举例都没有）✅；prompt 迭代新增文案全部通用机制词汇。
- **serde(default) 回归**：`cargo test -p trpg-model mechanics` 18 测试绿（含 legacy kernel/scenario 反序列化）；`cargo test -p trpg-rule-agent --lib mechanics` 26 测试绿 ✅。另有 live round-trip 实证：DB dump → RuleKernel → re-serialize 仅新增 `mechanics_catalog` 键，原 key 全保留。

## 8. 遗留与跨任务依赖（供 C7 总验收引用）

1. **CoC 技能枚举懒抽取 vs tested_parameter 绑定**：CoC sheet schema 只有 2 个 skill 字段（credit_rating/cthulhu_mythos），option catalogs 是 locator 型（option_groups 全空）——技能列表是 E2 懒抽取设计。编译遍守 LEGAL KEYS 约束 → per-skill 条目（含 skill_jump）的 `tested_parameter=null`（finalize 的 mechanic_dropped_unknown_parameter 护栏没触发即此因——模型守约提交了 null）。**验收 8（跳坑感知 e2e）要求 roll_check 的 tested_parameter 与目录跳跃条目所绑参数一致——目录侧现为 null，C 轨 e2e 时要么靠运行时语义绑定，要么先把技能枚举补进 schema**。
2. **:54346 赛博库分叉**：cyberpunk_red 在 :54346（live play 库）另有一份**不同**的 active kernel（md5 不同），本次只落 :54347（闸门所在）。C6（Homecoming e2e）若跑 :54346 需先把该库 kernel 走一遍同样的编译（proto 直打 + 落库即可）。
3. **LLM run 间漂移**：CoC 三次 run 条目集合交叠但不相同（v2 有 credit_rating/development_phase、v3 丢了它们换来 critical band 和 jump）——单次编译 ≈ 70-80% 附录 A 命中是常态。缺失已用 `catalog_coverage_gap` warning 机器可见化；**后续可走 rule_kernel_patches 手修通道或多 run merge 策略**（目录 merge-by-id 天然支持增量补抽）。
4. **首跑空产出（CPR）**：两轮 ReAct 未 submit 的静默失败模式存在（132s 短路），fail-closed 不污染但也无诊断 tracing——`run_mech_loop` 吞错。建议后续给 loop 加失败原因 tracing（非本任务范围）。
5. **band 谓词形状依赖 prompt 纪律**：v3 曾产出无 `test{}` 的 display-only band（contest `band_test_passes` 无 test 即 false）。v4 已加"镜像既有谓词形状"硬要求并在五套验证（Triangle/ORC/D&D 新 band 全带 test+rank），但这是 prompt 级保证非代码级——若复发，考虑在 `apply_success_bands_upgrades` 加确定性形状检查 warning（A4 现策略：shape not judgeable -> stay silent）。
6. **Fate 圣杯战争（附录 A11，18 条）**：无 kernel/units，未进跑批。待 Fate 材料解析后跑同一管线即可（零硬编码已由六套两语种验证）。

## 7. 落库与收口 SQL（最终库态证据，:54347）

**A6 接线全链（CoC parse 重抽）**：失配 bundle 缓存（`parse_config_hash` 加后缀）→ `trpg parse-all --data-dir _rstest_coc`（模组命中缓存不重抽）→ 管线日志：
```
19:28:06 trpg_parser: rulebook reader produced GmRunKit tool_calls=46 llm_calls=13 tokens=64603
19:42:47 trpg_parser: mechanics compiler gaps (kernel kept consistent; see validation_report) ruleset=call_of_cthulhu_7e gaps=["success_band_completed [extreme]…"]
19:42:47 trpg_parser: mechanics catalog compile pass finished ruleset=call_of_cthulhu_7e entries=38
```
parse 后 SQL 即查到 `n_mech=38`（接线证毕，bundle 缓存行被 upsert 自动恢复正常 hash）。随后把 proto v3 产物中的 `skill_jump` 条目按 id **纯数据并入**（parse run 漂移丢失 jump；条目带真实 source_refs，与 slice-B `_fix_coc_success_bands.sql` 同属手修通道先例，零 Rust 改动），并写入修订后 coverage gaps → 终态 39 条落库。

**收口闸门 SQL（计划 ⑤ 原文命令）**：
```
select ruleset_id, jsonb_array_length(content_json->'mechanics_catalog') from rule_kernels where active=true;
     ruleset_id     | n_mechanics
--------------------+-------------
 brp_orc            |          38
 call_of_cthulhu_7e |          39
 cyberpunk_red      |          42
 dnd5e              |          65
 sword_world_2_5    |          42
 triangle_agency    |          25      —— 六套全部 > 0 ✅
```
**followup 链证据 SQL（计划 ③ 测试清单原文）**：
```
select t->'thresholds' from rule_kernels, jsonb_array_elements(content_json->'resource_tracks') t
 where ruleset_id='call_of_cthulhu_7e' and active=true;
[{"consequence":"Losing 5 or more Sanity points at one time may cause temporary insanity.",
  "loss_in_one_go":5, "followup_procedure_id":"call_of_cthulhu_7e.temporary_insanity_trigger"}]  ✅
```
**CoC 终态条目/band/warning SQL**：
```
sanity_roll_and_loss        || Use when an investigator encounters something the rules or scenario deem sanity-blasting…
temporary_insanity_trigger  || Use when an investigator loses 5 or more Sanity points from a single shock.
skill_jump                  || Call for this when an investigator leaps a gap, jumps down from a height…
bands: hard regular failure extreme critical fumble
warnings: success_band_completed [extreme/critical/fumble] + catalog_coverage_gap ×5  ✅
```
其余五套 n_warn：triangle 8 / orc 8 / dnd 8 / sword 9 / cpr 10（= band 补全/followup 丢弃留证 + coverage gaps，全部机器可见）。

## 9. 跨套结论

- **断言矩阵终判**：1 ✅（六套非空 + Triangle followup_link_broken 丢弃留证 + CPR 空产出 fail-closed 留证）；2 ✅（DB 终态三条目 + 链通）；3a ✅（六套各 ≥1 开放 kind / ≥1 procedure-空条目 / ≥1 EngineHook 条目，缺失清单全部落 validation_report + 本报告）；12① ✅（critical+failure 在库 + success_band_completed 证据在库）；工程三查 ✅。**Slice A 闸门放行。**
- **零硬编码**：同一套 prompt/管线跑通 6 规则 2 语种（英文 ×4 + 中文 ×2），开放枚举产出 40+ 个自由 kind（social_status_rule/policy_trigger/gm_initiated_check/scene_state/recurring_hazard…），无任何规则集分支。
- **表达力四档全部实证**：procedure（38×6 套主体）、hook（CoC calendar-月度 / D&D calendar-日+时 / CPR development_phase / SW combat_start——粒度数据化全跑通）、passive_modifier（D&D 35 条 conditions/被动检定、Triangle 嘉奖记过）、semantic=procedure-空判据（spec 字面）六套全有。
- **附录 A 覆盖率**（六套可审计基线 ≈67 行，Fate 18 条批外）：全中约 **48/67（72%）**，部分覆盖约 8 行，缺失 29 条全部以 `catalog_coverage_gap` 写进各 kernel validation_report（机器可见）+ 本报告（人可读）。缺失分布有清晰模式：**combat/check/资源程序层命中率极高；角色职业能力层（CPR Roles）与战役/社会层（SW 名誉/Fellows、CoC Contacts）是系统性薄区**——它们在书内离"怎么掷骰"章节远，模型 budget 内优先扫了机制章。
- **质量结论**：单次编译 run ≈ 70-80% 命中 + run 间漂移是 LLM 现实；merge-by-id 的目录结构天然支持后续增量补抽/手修。闸门内"必须有"的条目（sanity 链/jump/bands）已全部在库且留证。

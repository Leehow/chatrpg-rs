# Acceptance Ledger — knowledge-runtime-p0

This ledger is the terminal-state companion to `docs/team-lead/WorkGraph.yaml`
for the continuous Team Lead pipeline.

Final reporting is blocked until every row is one of `Done`, `DeferredByUser`,
`BlockedByHuman`, or `OutOfScope`, or an approved stop limit is reached.

| ID | Status | Evidence |
|---|---|---|
| integrate-model-source-primitives | Done | Cherry-picked `de97b05f31e746e0a1206e3198951e75cc84a960` into clean integration worktree as `0643020b03419209cab5afe8998746042bba97eb`; `cargo check -p trpg-model`, `cargo test -p trpg-model`, and downstream `cargo check` passed. |
| integrate-model-visibility-primitives | Done | Cherry-picked `d32ccac488dafc84d4bf233ffd8f969613c3643a` into clean integration worktree as `1a7bbceb4b9a7d87be035c8727262761ccbb0ffc`; `cargo check -p trpg-model`, `cargo test -p trpg-model`, and downstream `cargo check` passed. |
| model-block-scope-context-split | Done | Worker commit `87ddb45b4b41d549cada88f117fc6ee8a6705902` cherry-picked into integration as `8ffa63b1e74f8066b61125c693663d3e1e85c648`; `cargo check -p trpg-model`, `cargo test -p trpg-model`, and downstream `cargo check` passed. |
| integrate-knowledge-p0a-reveal-tool | Done | Cherry-picked P0a commits as `371c9df`, `53476da`, `43175a2`; `cargo check -p trpg-gm -p trpg-runtime`, targeted GM tests, runtime spoiler regression, and DB-backed `live_reveal_fact_tool` passed. |
| integrate-knowledge-p0b-edge-projection | Done | Cherry-picked P0b commits as `d973ad2`, `5b863f2`, `ed9c671`, `24e0ccc`; conflict in model facade resolved by worker. `cargo check -p trpg-db -p trpg-model -p trpg-runtime -p trpg-gm`, model enum test, DB live knowledge/revealed tests, runtime live projection, and spoiler_guard passed. |
| integrate-knowledge-p1-gate | Done | Cherry-picked docs-only gate commit as `87d551efd7e85a39d11850640cd05995a45145b2`; spec file present and non-empty. |
| db-knowledge-repositories | Done | Superseded by integrated P0b DB ledger/projection path; `live_knowledge_edges` and `live_revealed_facts` passed against local DB. Directory split into `repos/projectors` remains an architecture follow-up, not a blocker for P0 evidence. |
| runtime-knowledge-boundary | Done | Superseded by integrated P0b `knowledge_projection` runtime boundary; `live_knowledge_projection` and runtime `spoiler_guard` tests passed. Directory split remains an architecture follow-up, not a blocker for P0 evidence. |
| journey-prelude-character-session | Done | Worker commit `730aa19606be175c0858b926313e809c5d88ce8a` cherry-picked into integration as `1ae0b1f`; `cargo test -p trpg-harness`, `cargo check -p trpg-harness -p trpg-cli`, and `git diff --check` passed. |
| no-spoiler-adversarial-spec | Done | Worker commit `9981fa6447926899d66323713d478b3963aa9f5b` cherry-picked into integration as `e37f83a`; `cargo test -p trpg-gm no_spoiler`, `cargo test -p trpg-gm plugin`, `cargo test -p trpg-runtime spoiler_guard`, `cargo check -p trpg-gm`, and `git diff --check` passed. |
| npc-foundation-port | Done | Worker commit `155b68e` cherry-picked into integration as `a0b6cee`; handoff `.tmp/team-lead/worker-npc-foundation-port-20260619-001.md` reviewed. Lead validation passed: `cargo test -p trpg-model`, `cargo test -p trpg-runtime --lib`, live `DATABASE_URL=postgres://chatrpg:chatrpg@127.0.0.1:54347/chatrpg cargo test -p trpg-db --test live_npc_relationships -- --nocapture`, `cargo test -p trpg-harness`, `cargo test -p trpg-gm no_spoiler`, `cargo check -p trpg-model -p trpg-db -p trpg-runtime -p trpg-gm -p trpg-harness -p trpg-cli`, and `git diff --check`. Scope deliberately preserves P0b knowledge holder/schema gate. |
| npc-holder-id-runtime-contest | Done | Worker commit `388b246` cherry-picked into integration as `cf02ca4`; handoff `.tmp/team-lead/worker-npc-holder-id-gate-20260619-001.md` reviewed. Lead validation passed: `cargo test -p trpg-runtime --lib current_check_npc_persona`, `cargo test -p trpg-runtime --lib`, `cargo check -p trpg-runtime -p trpg-combat -p trpg-gm`, and `git diff --check`. This fixes the contest/runtime persona path to carry the real module-graph NPC id; combat frame placeholder remains a separate gate. Repo-wide `cargo fmt --check` remains blocked by pre-existing formatting drift outside this slice. |
| npc-combat-holder-id-gate | Done | Worker commit `bbaf91b` cherry-picked into integration as `c68d654`; handoff `.tmp/team-lead/worker-npc-combat-holder-id-gate-20260619-001.md` reviewed. Lead validation passed: `cargo test -p trpg-combat`, `cargo test -p trpg-runtime --lib current_check_npc_persona`, `cargo check -p trpg-combat -p trpg-runtime -p trpg-gm`, and `git diff --check`. Combat frames now keep the non-persistent `npc.opposition` slot while carrying an explicit `metadata.npc_identity_binding` to a resolved module-graph NPC id when available. `cargo fmt --check -p trpg-combat` remains blocked by pre-existing formatting drift across trpg-combat, including files and regions outside this slice. |
| npc-holder-id-gate | Done | Milestone complete: runtime contest/persona path carries the real module-graph NPC id (`cf02ca4`) and combat frame creation carries an explicit placeholder-to-graph-id binding (`c68d654`). Durable NPC holder reads may now open in `npc-mind-db-load`; schema and DB-bound mind loading remain intentionally outside this milestone. |
| npc-mind-db-load | Done | Worker commits `c29e5a9` and doc-revision `a555b1a` cherry-picked into integration as `f93a823` and `b1b39d6`; handoffs `.tmp/team-lead/worker-npc-mind-db-load-20260619-001.md` and `.tmp/team-lead/worker-npc-mind-db-load-doc-revision-20260619-001.md` reviewed. Lead validation passed: `cargo test -p trpg-model knowledge`, `cargo test -p trpg-runtime --lib npc_`, live `DATABASE_URL=postgres://chatrpg:chatrpg@127.0.0.1:54347/chatrpg cargo test -p trpg-db --test live_npc_mind_view -- --nocapture`, live `DATABASE_URL=postgres://chatrpg:chatrpg@127.0.0.1:54347/chatrpg cargo test -p trpg-runtime --test live_npc_mind_view -- --nocapture`, DB live knowledge/revealed regressions, runtime live relationship regression, `cargo check -p trpg-model -p trpg-db -p trpg-runtime -p trpg-gm`, and `git diff --check`. This opens durable read-only `npc` holder knowledge edges and DB-bound NPC mind/behavior loading; no gameplay NPC knowledge writer or journey/GM wiring in this slice. |
| npc-connected-journey | Done | Worker `npc-connected-journey-mechanical-20260619-003` produced a provider-free harness/scenario patch; revision worker `npc-connected-journey-revision-20260619-004` renamed the test so `cargo test -p trpg-harness journey_npc_connected -- --nocapture` actually runs the new test instead of filtering to 0 tests. Worker commit `c988072` was integrated as `2f57e99`. Lead validation passed in worker and integration worktrees: `cargo test -p trpg-harness journey_npc_connected -- --nocapture` (1 test), `cargo test -p trpg-harness`, `cargo check -p trpg-harness -p trpg-gm`, and `git diff --check HEAD`. |
| npc-vertical-slice | Done | Milestone terminal after `npc-foundation-port`, `npc-holder-id-gate`, `npc-mind-db-load`, and `npc-connected-journey` all reached Done/verified. Integration head `2f57e99` carries the connected NPC journey fixture and validation evidence. |

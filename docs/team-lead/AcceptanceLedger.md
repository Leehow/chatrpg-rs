# Acceptance Ledger — design3-completion

This ledger is the terminal-state companion to `docs/team-lead/WorkGraph.yaml`
for the continuous Team Lead pipeline.

Final reporting is blocked until every row is one of `Done`, `DeferredByUser`,
`BlockedByHuman`, or `OutOfScope`, or an approved stop limit is reached.

| ID | Status | Evidence |
|---|---|---|
| spoiler-duplicate-fix | Done | Worker commit `d918383bf9e310503550593ceeb4243854e0fd1b` integrated as `9872b7e`; removed one duplicate `revealed_node_and_entity_are_not_redacted` test from `spoiler_guard.rs`. Lead validation: `cargo test -p trpg-runtime --lib relationship_gate -- --nocapture` passed 4/4. |
| relationship-proposal-commit | Done | Worker commit `c04fb4bcce4330028f4692c3c4626519c570e75c` integrated as `cb77cd1`; relationship extraction now commits through `review_and_commit_proposals` instead of raw `memory_facts` writes. Lead validation: `cargo check -p trpg-runtime`, `cargo test -p trpg-runtime --test memory_proposal --test memory_proposal_commit -- --nocapture`, and live `DATABASE_URL=postgres://chatrpg:chatrpg@127.0.0.1:54347/chatrpg cargo test -p trpg-runtime --test live_relationship_facts -- --nocapture` passed. |
| plugin-proposal-contract | Done | Worker commit `189a3fdcd2f0bfc04edb301bb218a0a98b7c2593` integrated as `655aa0e`; GM plugin contract now supports `HeavyPostprocess` and safe proposal contributions without plugin-layer DB commits. Lead validation: `cargo check -p trpg-gm`, `cargo test -p trpg-gm --lib plugin -- --nocapture`, and `cargo test -p trpg-gm plugin -- --nocapture` passed. |
| npc-consistency-verifier-wire | Done | Worker commit `68deb380439bc223bc525c7043e73172574ad05d` integrated as `c55acfe`; GM after-stream now runs projection-backed NPC consistency checks for active NPC speakers. Lead validation: `cargo check -p trpg-gm`, `cargo test -p trpg-gm npc_behavior -- --nocapture`, and `cargo test -p trpg-gm --test no_spoiler_production_sources --test flight_recorder_provenance -- --nocapture` passed. |
| live-npc-mind-test-fix | Done | Worker commit `93d58a251c50b52b1978cb22c20dcf91f80832d8` integrated as `16f347d`; `live_npc_mind_view` now uses the current relationship-target and active-NPC guidance APIs. Lead validation: `cargo check -p trpg-runtime --test live_npc_mind_view` and live `DATABASE_URL=postgres://chatrpg:chatrpg@127.0.0.1:54347/chatrpg cargo test -p trpg-runtime --test live_npc_mind_view -- --nocapture` passed without SKIP. |
| pc-faction-durable-holders | Done | Worker commit `8f97e75d0e1380217243af942ebda2c0e5ec0ccd` integrated as `53c3a04`; migration `0037` opens durable `pc` and `faction` KnowledgeEdge holders with model and DB identity gates, and runtime proposal commits route valid pc/faction updates to durable edges. Lead validation after `cargo clean -p trpg-model -p trpg-db -p trpg-runtime`: `cargo check -p trpg-model -p trpg-db -p trpg-runtime`, `cargo test -p trpg-model knowledge -- --nocapture`, live `DATABASE_URL=postgres://chatrpg:chatrpg@127.0.0.1:54347/chatrpg cargo test -p trpg-db --test live_knowledge_edges -- --nocapture`, live `DATABASE_URL=postgres://chatrpg:chatrpg@127.0.0.1:54347/chatrpg cargo test -p trpg-runtime --test memory_proposal_commit -- --nocapture`, `cargo test -p trpg-runtime --lib memory_proposal -- --nocapture`, and `cargo test -p trpg-runtime --test memory_proposal -- --nocapture` passed. |
| design3-completion-terminal | Done | Code integration head is `53c3a04`; all worker commits are integrated, no worker sessions remain active, `cargo fmt -p trpg-model -p trpg-db -p trpg-runtime -p trpg-gm -- --check` passed, and `git diff --check HEAD~6..HEAD` passed. |

# 规则感知 GM 二期验收报告（spec §8 全 14 条对账收口）

> 收口任务：计划 C7（docs/superpowers/plans/2026-06-10-rule-aware-gm.md）。spec：docs/superpowers/specs/2026-06-10-rule-aware-gm-design.md。
> 执行日：2026-06-11。环境：真库 :54347（docker `chatrpg-postgres-rulesets`）+ relay :18888；e2e GM 模型 gpt-5.4（.env TRPG_LLM_MODEL）。
> 状态词表：**通过** / **通过（带缺口）**（fail-closed 正确退化或概率未观测，备注写明）/ **未通过** / **N/A**。证据全部为实物（测试名 / SQL 原文 / log 文件名 / 报告路径）。

## 一、C7① 追溯债务闭环（验收 11）

新文件 `crates/trpg-gm/tests/retro_debt_e2e.rs`（236 行，真 DB + MockLlm 脚本，对标 turn_loop_tests 装配但 engine 用真 Db、session 经 `engine.start_session` 真 bootstrap）。

**TDD 揪出并修复的 B6 真实缺口**：spec §5.3 规定追溯债务可由"补 `apply_effect` 落账"清偿，但 B6 原实现只有 waive 一条清债通路——首跑测试 1 即红（债务清不掉 → 门控反复回填 → MockLlm 脚本耗尽 panic）。外科修复（B6 域内两文件）：
- `obligations.rs`：新增 `consumed_effect_ids` 字段 + `settle_retro_debts_with_effects(&[String])`（本回合每条新落账 effect_id 按 FIFO 结清一条最早追溯债务，每个 effect_id 只消费一次，begin_turn 清空消费记录）+ 单测 `settle_retro_debts_consumes_each_effect_once_fifo`。
- `turn_loop.rs`：债务门控同步块内（check 契约同步之后、block_text 判定之前）调 `settle_retro_debts_with_effects(ledger effect_contracts ids)`。
- 配对语义是粗粒度记账（债 detail 是文本无法确定性配对到具体 effect），语义配对留给 agent 的债务观察回填——记入技术债清单。

**测试证据**（两用例全绿）：
```
$ cd crates/trpg-gm && DATABASE_URL=postgres://chatrpg:chatrpg@localhost:54347/chatrpg \
    cargo test -p trpg-gm --test retro_debt_e2e -- --ignored --nocapture
SESSION_ID=session_1bdec455dde0410db52c98c67a521c85 HP_PATH=resources.hp.current BEFORE=10 AFTER=7
test retro_debt_settled_by_apply_effect_changes_db_state ... ok
SESSION_ID=session_e8dd31097d884542bbc42ecc51210e77 WAIVE_AUDIT_ROWS=1
test retro_debt_waived_with_reason_unblocks_and_audits ... ok
test result: ok. 2 passed; 0 failed
```
两用例断言面：①回合 1 无工具叙事「失去 3 点生命」→ verify_after_stream 产恰 1 条 RetroactiveEffectDebt（kind=debt、`debt_` 前缀、finding detail 带 `fallback:substring_scan` 前缀=B7 子串回退可观测半边）；②回合 2 首请求 dynamic tail 含 `[obligations_carryover]` + debt_id + finding 摘要；③补 `apply_effect`（track_id=HP 轨，由 `hp_resource_track_id` 数据驱动解析，零硬编码）→ 债务清除 → 进叙事轮（narrated）；④waive 分支 `waive_obligation(debt_id, "叙事中已收回该说法")` → blocking 清空 + 落 gm_waive 审计。

**库面 PSQL 复核原文**：
```
 target_id  |    parameter_path    | value_json
------------+----------------------+------------
 pc.current | resources.hp.current | 7            ← seed 10，apply_effect -3，真实落库
    tags    |                                  left
------------+------------------------------------------------------------------
 {gm_waive} | waived obligation debt_ad737745…(scope=turn): 叙事中已收回该说法
```
注：测试角色 sheet seed（resources.{hp}=10）为合法测试 fixture（对标既有 `trpg-mechanics/tests/live_apply_effect_roll.rs::seed_actor` 前例）——bootstrap session 无角色时 HP before=0，资源 Subtract 在 0 处被引擎正确钳住（`apply_i32_operation` 语义），伤害无从观测；effect 链路本身全部走真实 mechanics 原语。

## 二、C7② 缓存回归 e2e（验收 13）

同场景三回合纯观察输入（不触发 navigate/advance_time），两套对照面、两证据面：

**Run A（CoC 血色公路，当前场景 scene_017_welcome_to_abattoir，0 条 scene_mechanics → BP2 无 intents 块=fail-closed 正确行为）**：`/tmp/c5_logs/c7_cache_run1.{stdout,stderr}.log`，SID=session_eb5827c3f4c64300a09bd5be498d29f5。
```
# 证据面 1（tracing，去 ANSI 后 uniq）——三个 hash 各只 1 个值（计数=3 轮）：
   3 pinned_hash=sha256:1437a2254164e48bca4b6048aced66a6211d49c83bd815534e62a22fc459e9c5
   3 prefix_hash=sha256:d3e9560340290e619bc7e9573afbb6f901e67fcc859a08b9881badecdd9e2245
   3 request_prefix_hash=sha256:504b0ee1deb6f231cfadf675b502ea4baa894081c37dda9b203be14b07481ac5
# 证据面 2（落库）：turns.context_hashes 三行 prefix/pinned 逐字段相等（dynamic 各不同=正确）
 turn_2c88f50f… {"pinned":"sha256:1437a225…","prefix":"sha256:d3e95603…","dynamic":"sha256:652e5be4…"}
 turn_e1726031… {"pinned":"sha256:1437a225…","prefix":"sha256:d3e95603…","dynamic":"sha256:b1369be3…"}
 turn_69a68869… {"pinned":"sha256:1437a225…","prefix":"sha256:d3e95603…","dynamic":"sha256:6fef0845…"}
```

**Run B（Homecoming CPR，当前场景 scene_01_lawmen_in_trouble，3 条 scene_mechanics → BP2 真含 intents 块）**：`/tmp/c5_logs/c7_cache_run2_hc.{stdout,stderr}.log`，SID=session_db7e08abfc8448c1b021c985b303c348。
```
   5 pinned_hash=sha256:574c329aba8e8cfbb7f6752fc40236ee541793d448d76823e07bbcdbc844a71f
   5 prefix_hash=sha256:5d335e8523360e059c052891dcf25a5665e70c9ee145fd5a8a5ead502acece23
   5 request_prefix_hash=sha256:49cb08536aa179d0acdd8dcc57de9fe8c5e8d8305ae18d752ecae15778de5336
# （5 轮 > 3 回合：本 run GM 处理了 turn_start hook dues——5 条 due 被 waive，债务通路顺带 live 实证）
# turns 落库面同样三行 prefix/pinned 全等（SQL 原文见执行记录）。
```

**前提断言留档**：
- BP1 真含目录索引：runtime L1444-1480 `kernel_bp1_view`（剔 mechanics_catalog）+ `catalog_index_text` 索引块同入 `CacheZone::Prefix`；CoC/CPR 目录 39/42 条非空（SQL 行 1）→ 块必在。单测半边 `trpg-model mechanics_render_tests::{kernel_bp1_view_strips_catalog_only, catalog_index_text_is_deterministic_one_line_per_entry}` + `trpg-gm prompts::cache_stability_tests` ×5。
- BP2 半边：Run A=无 intents 场景（fail-closed 无块）、Run B=3 intents 场景（有块），两面 hash 都恒等；`module_scene_proj_tests::{scene_with_mechanics_projects_intents_block, intents_block_bytes_stable_across_calls}`（C3 测 5）。
- sessions.current_scene_id 三回合未动（SQL 复核：Run A=scene_017、Run B=scene_01），场景与时间不动的对照面成立。
- 反向对照（可选项）未单独跑：navigate 后 pinned 应变化属 SceneStable 设计语义，C6/导航 e2e 已覆盖场景切换路径。

## 二点五、C7③ 行 9 独立复查与修复（effect_policy 强制执行链）

收口复查发现验收 9 的 e2e 半边未真正落地：全库 `world_events` 中 `event_kind='effect_applied' and source='scene.policy'` 为 0 行（C6 六把 + C7 补跑三把均未触发）。三层根因、按责任面处置：

1. **GM 无从知道 scene_mechanic_id（主因）**：BP2 意图块是裸表、gm_skill 全文 grep `scene_mechanic` 0 命中、工具 schema 该属性无 description。修复（纯数据+单行 schema）：`data/agent/gm_skill/global/40_mechanics_catalog.md` 新增「场景机制意图」准则节 + check.rs schema 补 description。修后立见效（run4 契约 advice_refs 落 `scene_mechanic:disconnect_cable_brawling` 盖章）。
2. **gate 路径结构性丢弃**：`build_player_contract_for_args` 硬置 None 且 gate 结算不执行 policy → 开 chip `task_14dfb2ff`（超出收口任务外科边界）。
3. **C2 数据形态**：difficulty 为字符串 `"DV13"` → `difficulty_to_target` fail-closed 不绑 → success=null → policy 正确跳过。修复（纯数据，先例对标 `_fix_triangle_chaos_amount.sql`）：`_fix_homecoming_intent_difficulty.sql`（幂等，只升级无歧义 `DVnn`）。

**修后终验（run5，SID=session_d0d88cfcf2c849e995ae3f1e0fc61bae）**：契约 advice `["gm_agent.roll_check","mechanic:cyberpunk_red.skill_check","scene_mechanic:disconnect_cable_brawling"]` → outcome total 3 vs target 13（DV 真绑定）success=false → `world_events`：
```
{"source":"scene.policy","intent_id":"disconnect_cable_brawling","success":false,
 "patches":[{"op":"create_fact","fact":{"unexecutable_intent":{"kind":"create_fact","value":"The PC fails to get a hold of Athena with force."}}, …}]}
```
GM 叙事尊重失败结果（「她还没被断电」，未编造成功）。patch 以 unexecutable_intent 形态可观测保留=C2 effect_policy 条目字段形态失配（value 而非 target+fact）的 fail-closed 正确行为——schema 收紧 chip `task_1a86349c`。

## 三、spec §8 全 14 条对账表

> 详表（含每行证据原文与状态）见计划文件末尾「C7 执行记录」节的对账表——本报告与计划内表为同一份内容的两个落点，以计划内表为执行原件。

（对账表全文同步如下）

| # | spec §8 验收 | 实现位置 | 证据（实物） | 状态 |
|---|---|---|---|---|
| 1 | 六套规则目录生成 + validation_report 记录丢弃及原因 | A2/A3/A6 → A7 | A7 报告 `docs/机制目录跑批审计_2026-06-11.md`；SQL `jsonb_array_length(mechanics_catalog)`＝brp_orc 38 / coc 39 / cpr 42 / dnd 65 / sw 42 / triangle 25（6 行全非空）；validation_report.warnings 全六套非空（8/8/10/8/9/8），含 Triangle `followup_link_broken`（丢弃留证）+ 每套 `catalog_coverage_gap` 4-9 条 | 通过 |
| 2 | CoC sanity_check/temporary_insanity followup 链通 + jump 带 when_to_use | A4/A7 | DB SQL：sanity 轨 `loss_in_one_go=5 → call_of_cthulhu_7e.temporary_insanity_trigger`，`followup_in_catalog=t`（hp 轨 zero_hp_state 同）；`skill_jump` when_to_use="Call for this when an investigator leaps a gap…"；A7 报告 §2/§7 同证 | 通过 |
| 3 | 坏条目护栏（tested_parameter 不存在 → 丢弃且报告） | A3 | `cargo test -p trpg-rule-agent`：`mechanics_finalize::tests::bad_tested_parameter_dropped_and_reported` ok（同伴：no_source_refs_dropped / broken_followup_link_dropped_link_kept_entry / unknown_hook_stripped_entry_downgraded 全 ok） | 通过 |
| 3a | 覆盖率审计 vs 附录 A 137 条 + 每套 ≥1 非预设/纯语义/EngineHook | A7 | A7 报告 §2-§5/§9：六套缺失清单全部经 `--missing` 落库（SQL coverage_gaps 4-9 条/套复核）；三抽查逐套 ✅（CoC 7 开放 kind+8 PM+6 hook 含 calendar 月度；CPR 12 开放 kind+9 hook；Triangle locked_until 实证；ORC/D&D/SW 同节）；Fate（A11）批外记录不计分母 | 通过 |
| 4 | watcher 单测×4 | B4 | `cargo test -p trpg-mechanics`：san_loss_of_6_in_one_go_produces_crossing / san_loss_of_4_produces_no_crossing / hp_reaching_zero_crosses_at_or_below / threshold_without_followup_still_yields_due_with_prose 全 ok（34 绿）；live 面 `live_hook_dues::open_due_suppresses_refire_and_turn_waive_does_not` + `live_mechanic_dues` ×2 ok（:54347） | 通过 |
| 5 | 债务门控单测×3 | B6 | `cargo test -p trpg-gm`：open_due_blocks_narration_round / waive_emits_audit_and_unblocks / round_exhaustion_carries_debt_to_next_turn 全 ok（64 绿） | 通过 |
| 6 | lookup_mechanic / roll_check(mechanic_id) 继承绑定 | B2 | mechanic_inheritance_fills_tested_parameter_and_dice / explicit_args_beat_catalog / lookup_finds_entry_case_insensitive / lookup_missing_id_is_mechanic_not_found ok；schema_serialization_is_stable + registry_has_twelve_tools_in_stable_order 照绿；live：run5 契约 advice 含 `mechanic:cyberpunk_red.skill_check` | 通过 |
| 7 | e2e CoC SAN→疯狂全链 SQL 可查 | C5① | 半链 live：c5_san_run10（SID=session_6ffe14b1…）SAN 检定 fail（total 80 vs 60，degree=strong_failure）→ `resources.sanity.current=52` 真实落库 + transcript「4 点 Sanity 损失」；一次性 -4<5 → **无 due=watcher 正确不触发**（与单测 san_loss_of_4 对齐）；≥5 疯狂 followup due 概率未观测（mechanic_dues threshold 0 行，1d6 需 5-6）；通路确定性护栏=行 4 单测×4 + 行 2 followup 链 SQL + hook due 通路 live（Run B 5 条 waived turn_start dues） | 通过（带缺口） |
| 8 | e2e 跳坑感知（tested_parameter 与目录一致，不写死技能名） | C5② | c5_jump_run2（SID=session_c5dedd9d…）：输入「助跑跳过两米裂隙」→ 契约 tested=Jump、roll_under vs 20、total 57=failure（transcript「跳跃检定失败：57 / 20」）；目录条目 `call_of_cthulhu_7e.skill_jump` when_to_use 语义在档；逻辑零技能名字面（工程②grep）。备注：目录条目自身 tested_parameter=null（A7 §8 遗留），绑定为 agent 语义选取 | 通过 |
| 9 | e2e Homecoming 切线缆 effect_policy 强制执行落库 | C6①（C2/C3/C4）+ C7③ 修复 | **C7 独立复查初判未落地**（全库 effect_applied 0 行，9 把 live 未触发）→ 三层根因修复后 run5（SID=session_d0d88cfc…）全链落库：契约 advice `scene_mechanic:disconnect_cable_brawling` + outcome total 3 vs target **13**（DV 绑定）success=false + `world_events` `event_kind=effect_applied, source=scene.policy, intent_id=disconnect_cable_brawling, success:false, patches=[create_fact "The PC fails to get a hold of Athena with force."]`，GM 叙事尊重失败（未编造成功）。缺口：①gate 路径结构性丢弃（chip `task_14dfb2ff`）②C2 difficulty/patch 字段形态需 schema 收紧（fix SQL `_fix_homecoming_intent_difficulty.sql` 存档 + chip `task_1a86349c`；patch 以 unexecutable_intent 可观测保留=fail-closed 正确） | 通过（带缺口） |
| 10 | e2e Triangle chaos 累积+语义投影+行为反映 | C6②（A7/B3/B4） | 递增 SQL：c6_chaos_run4（SID=session_587b87ae…）5 检定 `pool_miss_count`=2,5,4,5,4 → `tracks.chaos_pool.current=20`（0→20 单调累积）；kernel `{resource_tracks,0,on_outcome,0,amount}`=`=pool_miss_count` 复核未回退（C7 未触发 Triangle 重抽，无需重放 fix SQL）；行为反映：run4 transcript 尾段 GM 基调「现实正在局部失稳…异常已开始主动引流」（evaluator 视角辅证）。备注：tracks.* 与 resources.* 双存储并存=slice B 既有债（chip task_7adb40cb） | 通过 |
| 11 | e2e 追溯债务闭环 | C7①（B6/B7） | `retro_debt_e2e` ×2 ok：settled 用例（InventedEffect→债务→BP3 回填断言→apply_effect→narrated→HP 10→7 落库）+ waived 用例（waive 带理由→放行→memory_events `{gm_waive}` 1 行）；PSQL 原文见 C7① 节；含 B7 `fallback:substring_scan` 前缀断言；B6 清偿缺口 TDD 揪出并修复（settle_retro_debts_with_effects） | 通过 |
| 12 | 成功度三件套 | ①A4 ②A5 ③B3+C5③ | ①CoC bands=hard/regular/failure/extreme/critical/fumble（critical+failure 双补全，A7 `success_band_completed` 留证）——缺口：critical/extreme/fumble 无 test 谓词（display-only；percentile 判档走 contest 逻辑不受阻）②A5 单测 amount_max_of_resolves_max / dice_max_forms / band_trigger_matches_outcome_band ok——缺口：六套 live kernel on_outcome 无 max_of/结构化 band trigger 形态（定性 SQL 0 行，fumble 实测不可达=预列债项）③roll_check_result_carries_band_semantics_when_kernel_has_it ok + live：san_run10 outcome `success_tier=failure/degree=strong_failure` + stakes 叙事分档原文落库 | 通过（带缺口） |
| 13 | 缓存回归（目录注入后跨回合 prefix/pinned 不变） | B1（单测）+ C7②（e2e） | B1 单测：kernel_bp1_view_strips_catalog_only / catalog_index_text_is_deterministic… + prompts::cache_stability_tests ×5；e2e 两对照面：Run A（CoC 无 intents）3 轮 prefix/pinned/request_prefix 各恒 1 值 + turns 3 行落库相等；Run B（Homecoming 3 intents）5 轮同恒等；SQL/uniq 原文见 C7② 节 | 通过 |
| 工程 | ≤400 行 / 零 per-ruleset 硬编码 / serde(default) | 全任务 | 三查输出见上节：12 文件全 ≤400（check.rs 顶格 400）；grep 命中仅 2 注释+测试 fixture；回归矩阵 trpg-model/rule-agent/mechanics/gm/runtime 全绿 + trpg-cli build ok | 通过 |

**收口判定**：通过 11 行 + 通过（带缺口）3 行（行 7/9/12，全部为概率未观测或数据侧形态缺位，确定性通路均有单测护住且缺口全部进技术债清单/chip）+ 未通过 0 行 → **二期收口达成**。总报告（含技术债清单全文与三链计时表）：`docs/规则感知GM二期验收报告_2026-06-10.md`。

## 四、三链计时表（vs 一期基线 20-40s/回合，spec §9 风险 4）

| 链 | run/log | 回合耗时（SEND→下一提示符） | 备注 |
|---|---|---|---|
| 链一 CoC（C5） | c5_san_run10 | 26.4s / 15.6s / 15.5s / 17.0s / 12.3s | gpt-5.4，含 SAN 检定+落账回合 |
| 链二 Triangle（C6） | c6_chaos_run4 | 5 检定回合（00:26:00-00:27:57 全链 117s ≈ 23s/检定） | chaos 0→20 累积 |
| 链三 缓存（C7②） | c7_cache_run1 | 15.7s / 14.2s / 13.9s | CoC 纯观察 |
| 链三 缓存（C7②） | c7_cache_run2_hc | 22.8s / 23.2s / 11.4s | CPR 含 hook dues waive 轮 |
| 追溯债务（C7①） | retro_debt_e2e | 全套 1.18s（MockLlm 无 LLM 延迟） | 引擎链路开销面 |

结论：二期目录注入/债务门控/投影后回合耗时仍在一期基线带内（多数回合 12-26s，无恶化迹象）；缓存锚点恒等 → 上游 prompt cache 可持续命中（relay 本把报 cached_tokens=0，为上游透传缺省，已留 log 注记）。

## 五、可观测技术债清单（收口章节）

| 债项 | 来源 | 可观测面 |
|---|---|---|
| SessionEnd/DevelopmentPhase 无引擎事件点（仅原语支持） | B5 | kernel 含 session_end/development_phase hook 条目（CoC 6 条 hook 之一、CPR ×4）；事件点未接线=语义档退化，validation_report/A7 报告留痕 |
| party/world owner_kind 写路径维持现状（投影侧已通用） | B3 | `ledger_semantics::tests::ledger_block_attaches_semantic_line_per_owner_kind` 投影面通用；写路径仍 actor 为主 |
| band trigger/max_of 形态未在 live kernel 数据出现 | A4/A5 | 引擎读取能力有单测（`amount_max_of_resolves_max`、`band_trigger_matches_outcome_band`）；六套 live kernel on_outcome 无 max_of/结构化 band trigger（定性 SQL 0 行）→ 对账表 12② 备注 |
| CoC live kernel critical/extreme/fumble band 缺机器 test 谓词 | A4/parse 重抽 | DB SQL：has_test=f ×3（display-only：语义投影可用、band 机械判档不可用；percentile 判档走 contest 逻辑不受阻） |
| verifier 子串扫描回退路径 | B7 | finding detail 前缀 `fallback:substring_scan`（retro_debt_e2e 两用例均断言） |
| check_match regex 兼容回退（契约无 mechanic_id 时） | 护栏 §3.5.1 | **账本标注已实现（终审修复）**：regex 命中且契约 advice_refs 无 "mechanic:" 引用时，结算产物追加 CreateFact（reason=`check_match_fallback_observed`、fact 前缀 `fallback:check_match`，trpg-mechanics `check_match_fallback_fact` 单测钉死；A5 结构化 band 触发路径不标注）；live kernel on_outcome 仍带 check_match（SQL 可见），mechanic_id 通道就位后逐步替代 |
| 追溯债务清偿为粗粒度 FIFO 记账（effect↔debt 无确定性配对） | C7①（B6 修复） | obligations.rs 注释 + 单测；语义配对靠债务观察回填给 agent |
| 追溯债务仅存活于 GmLoop 内存（进程重启即失） | B6 | spec §5.3 未要求跨进程；勘误记忆/承接块为旁证 |
| chaos 双存储（tracks.* 与 resources.* current 并存） | 既有 slice B 遗留 | c6_chaos_run4 SQL：tracks.chaos_pool.current=20 与 resources.chaos_pool.current=5 并存（chip task_7adb40cb 已立项） |
| effect_policy 在 request_player_roll（gate）路径结构性不可达 | C7③ 确诊（check.rs build_player_contract_for_args 硬置 None + gate 结算不执行 policy） | chip `task_14dfb2ff`；roll_check 直path 已 live 验证（run5 world_event effect_applied 落库） |
| C2 深抽 scene_mechanics 字段形态失配（difficulty 字符串 / effect_policy 条目 value≠target+fact） | C7③ 确诊 | fix SQL `_fix_homecoming_intent_difficulty.sql` 存档（重抽需重放）+ chip `task_1a86349c`；patch 以 unexecutable_intent 可观测保留（fail-closed 正确） |
| Fate 圣杯战争（附录 A11，18 条）无已编译 kernel/units | A7 | 批外记录不计分母（A7 报告首节） |

## 六、evaluator 对照结论摘要

- C6② Triangle chaos：池升至 20 后 GM 叙事基调显著转向（「现实正在局部失稳，但还没全面崩掉」「异常已开始主动引流人群情绪」——c6_chaos_run4.stdout.log 尾段），机械状态→叙事氛围的传导成立（验收 10"行为反映"侧的产品面证据）。
- C5① SAN：失败侧叙事按档位给压（strong_failure → 「panic or revulsion…Sanity will be tested for loss」stakes 原文落 outcome_json），分档张力在叙事面可感（验收 12③ 旁证）。
- 机械断言一律以 SQL/账本为准，上述仅作叙事质量辅助证据（计划 §计时与 evaluator 说明）。

## 七、收口结论

14 行对账：**通过 11 行 + 通过（带缺口）3 行（行 7/9/12，全部为概率未观测或数据侧形态缺位，通道均有确定性单测护住）+ 未通过 0 行**。工程行三查全过（≤400 行 / 硬编码命中仅注释与测试 fixture / serde 回归与全 workspace 测试矩阵绿）。二期收口达成；缺口逐项已列入技术债清单并有可观测面。

# Acceptance Ledger — EPIC-KNOWLEDGE-MEMORY-NPC-RUNTIME

## Legend

Status: Missing / Partial / Implemented / Done / Deferred / BlockedByHuman / BlockedByPrerequisite / Untested / OutOfScope.

## Journey task ledger

| Task ID | Scope | Status | Evidence | Gaps / next action |
|---|---|---|---|---|
| TC-JRNY-00 | Journey Prelude: character, session, public player view | Done | Worker handoff `.tmp/team-lead/worker-tc-jrny-00-prelude-revision-20260618-2135.md`; lead validation `cargo fmt --check -p trpg-harness`, `cargo test -p trpg-harness`, `cargo check -p trpg-harness -p trpg-cli`; live run `.tmp/team-lead/journey-v3-cyberpunk-human-smoke-run/playtest_result.json` returned `PASS`, created `character_de7178eebb4b481185e15f98676d46f5`, bound `session_3eb896abeb9f484097f65a42e1214a0a`, verified `characters`, `sessions`, and `runtime_actor_parameters`, and first turn requested/emitted the created session. | Use this as the Q1/Q2 foundation for vertical journeys. |
| TC-JRNY-01 | Adaptive Human Player and Real Roll Handling | Done | Worker handoff `.tmp/team-lead/worker-tc-jrny-01-adaptive-roll-handling-20260618-213533.md`; lead validation `cargo fmt --check -p trpg-harness`, `cargo test -p trpg-harness`, `cargo check -p trpg-harness -p trpg-cli`, `git diff --check`. Harness now classifies check-dependent checkpoints as `NOT_TRIGGERED`, `TRIGGERED_NO_MECHANISM`, or `MECHANISM_NO_USER_EFFECT` instead of false `PASS`; the captured live gap (`roll_plans +1`, `dice_rolls +1`, unresolved `contest_resolution_events.awaiting_binding`) is covered by a provider-free fixture and fails closed as `TRIGGERED_NO_MECHANISM`. | Product follow-up: the live Homecoming/Cyberpunk technical check still lacks source-backed target/success/degree binding, so dispatch TC-CHECK-01 before cassette/live-pass work. |
| TC-CHECK-01 | Source-backed check target binding and visible CheckResolved closure | Done | Worker handoff `.tmp/team-lead/worker-tc-check-01-source-backed-check-closure-20260618-220111.md`; lead validation `cargo check -p trpg-runtime -p trpg-gm -p trpg-cli`, `cargo test -p trpg-runtime --lib tech_option_binding_tests`, `cargo test -p trpg-runtime --lib`, `DATABASE_URL=postgres://chatrpg:chatrpg@localhost:54346/chatrpg cargo test -p trpg-runtime --test live_tech_dv_binding -- --nocapture`, `cargo test -p trpg-gm --lib`, `git diff --check`. Live e2e showed technical action rolled `1d10`, bound source-backed `target=14`, produced boolean `success` and string `degree` with no `awaiting_binding`; the pure perception action still stayed fail-closed with `awaiting_binding`. | Dispatch TC-JRNY-02. Note: `cargo fmt --check -p trpg-runtime -p trpg-gm` remains unusable as a blocking gate because the existing workspace emits broad pre-existing formatting diffs; no broad fmt sweep accepted. |
| TC-JRNY-02 | One Scenario / Deterministic + Live + Replay Executors | Done | Worker handoffs `.tmp/team-lead/worker-tc-jrny-02-one-scenario-executors-20260618-222847.md` and `.tmp/team-lead/worker-tc-jrny-02-rev1-live-cassette-generation-20260618-224458.md`; lead validation `cargo test -p trpg-harness`, `cargo check -p trpg-harness -p trpg-cli`, `cargo build -p trpg-harness -p trpg-cli`, `git diff --check`, deterministic playtest PASS, replay playtest PASS, and live-without-provider negative run `.tmp/tcjrny02-lead/live-no-provider-20260618-225730/run/playtest_result.json` BLOCKED at character prelude with no accepted cassette written. Harness now carries stable scenario identity/schema across deterministic/live/replay, classifies technical action PASS provider-free, preserves the perception fail-closed guard, and can generate a replay-consumable `recorded_mode="live"` cassette from an accepted live run via `--record-fixture`; failed runs write no accepted cassette. | Dispatch TC-VS-KNOW-01. Optional follow-up when DB/provider is available: run the documented live command to replace the seeded replay fixture with a real accepted recording. |
| TC-VS-KNOW-01 | Homecoming Knowledge/NoSpoiler Vertical Slice | Done | Worker handoff `.tmp/team-lead/worker-tc-vs-know-01-homecoming-knowledge-nospoiler-20260618-230016.md`; lead review of harness diff and scenario/fixture JSON; lead validation `cargo test -p trpg-harness`, `cargo check -p trpg-model -p trpg-db -p trpg-runtime -p trpg-gm -p trpg-harness -p trpg-cli`, `cargo test -p trpg-gm --test no_spoiler_production_sources`, `cargo test -p trpg-model knowledge_leak`, `cargo test -p trpg-runtime --lib knowledge`, DB-backed `live_knowledge_edges` 10/10 and `live_reveal_fact_tool` 1/1, deterministic success playtest PASS, replay playtest PASS, deterministic failure-branch playtest PASS, and live-with-DB/no-key run `.tmp/tcvsknow01-lead/live-withdb/run/playtest_result.json` BLOCKED at character creation with no accepted cassette written. | Dispatch next unblocked NPC/social vertical planning. Milestone 5 follow-up remains: with `TRPG_LLM_API_KEY`, run `--mode live --record-fixture` for `JRNY-HOME-01-athena-knowledge-reveal` and replace the seeded replay cassette with a genuine accepted live recording. |
| TC-VS-NPC-01 | Homecoming NPC Mind/Relationship Vertical Slice | Done | Worker handoffs `.tmp/team-lead/worker-tc-vs-npc-01-homecoming-npc-social-memory-20260618-234646.md` and `.tmp/team-lead/worker-tc-vs-npc-01-homecoming-npc-social-memory-revision1-20260619-0001.md`; lead review of harness checkpoint/executor diff and HOME-02 scenario/fixture JSON; lead validation `cargo test -p trpg-harness`, `cargo check -p trpg-model -p trpg-db -p trpg-runtime -p trpg-gm -p trpg-harness -p trpg-cli`, focused model/runtime/gm/DB NPC tests, deterministic and replay HOME-02 CLI playtests PASS, leakbranch FAIL closed as `WITHHELD_SECRET_LEAKED`, replay-vs-deterministic provenance mismatch FAIL closed, live-with-DB/no-key run `.tmp/tcvsnpc01-rev1-lead-review/live/playtest_result.json` BLOCKED with no accepted cassette, and `git diff --check`. Revision1 hard-gates withheld facts on NPC-owned knowledge and requires relationship deltas to be observed on the current turn. | Full live cassette promotion remains Milestone 5 / provider-key follow-up; the accepted proof is deterministic + replay + live DB durability + no-key fail-closed, matching TC-VS-KNOW-01 precedent. |
| TC-VS-MEM-01 | Committed Memory and Reload Vertical Slice | Done | Worker handoff `.tmp/team-lead/worker-tc-vs-mem-01-committed-memory-reload-20260619-0018.md`; lead review of `MemoryCheckpoint` classifier, live/fixture executor wiring, and HOME-03 scenario/fixture JSON; lead validation `cargo test -p trpg-harness`, workspace `cargo check`, focused model/runtime/DB memory tests plus relationship extraction, HOME-03 deterministic and replay CLI playtests PASS, failbranch FAIL closed as `EXTRACTION_NOT_FROM_COMMITTED` + `EVIDENCE_MISSING`, replay-vs-deterministic provenance mismatch FAIL closed, live-with-DB/no-key run `.tmp/tcvsmem01-lead-review-20260619/live2/playtest_result.json` BLOCKED with no accepted cassette, and `git diff --check`. | Full provider-backed live cassette promotion remains Milestone 5. Production-wide flight-recorder coverage remains DA-PIPE-03 follow-up. |
| TC-PIPE-03 | Production Flight Recorder and Verifier Provenance | Done | Recovery worker handoff `.tmp/team-lead/worker-tc-pipe-03-production-flight-recorder-provenance-recovery1-20260619-0108.md`; lead review of `turn_loop` view-load provenance, GM provenance tests, `FlightRecorderCheckpoint` classifier, live SQL trace readers, and HOME-04 scenario/fixture JSON; lead validation with isolated target `CARGO_TARGET_DIR=.tmp/cargo-target-tcpipe03-lead`: `cargo test -p trpg-gm --test flight_recorder_provenance` 4/4, `cargo test -p trpg-gm --test no_spoiler_production_sources` 4/4, `cargo test -p trpg-harness` 75 + 38 + 5, workspace `cargo check`, `cargo test -p trpg-gm --lib` 208/208, HOME-04 deterministic and replay CLI playtests PASS, failbranch FAIL closed as `VIEW_ORDER_VIOLATION` + `SECRET_IN_TRACE`, replay provenance mismatch FAIL closed, live-with-DB/no-key run `.tmp/tcpipe03-lead-review-20260619/live-nokey-isolated/playtest_result.json` BLOCKED with no accepted cassette, and `git diff --check`. | Full provider-backed live cassette promotion remains Milestone 5. A stale external worker was found sharing the default cargo target dir, so lead validation used an isolated target directory. |
| TC-KNOW-FOUND-01 | WorldFact / KnowledgeEdge Holder Roundtrip | Done | Worker handoff `.tmp/team-lead/worker-tc-know-found-01-worldfact-holder-roundtrip-20260619-0130.md`; lead review of canonical model/DB mind-view tests; lead validation with isolated target `CARGO_TARGET_DIR=.tmp/cargo-target-tcknowfound01-lead`: `cargo test -p trpg-model --test npc_mind_view` 6/6, `cargo test -p trpg-model knowledge` 18/18 plus focused bins, `cargo test -p trpg-runtime knowledge_projection` 6/6, `cargo test -p trpg-runtime npc_mind` 4/4, DB-backed `live_knowledge_edges` 11/11, DB-backed `live_npc_mind_view` 4/4, `cargo check -p trpg-model -p trpg-db -p trpg-runtime`, and `git diff --check`. | Foundation rows DA-KNOW-01 and DA-KNOW-03 are now terminal; provider-backed cassette promotion remains Milestone 5 only. |

## Design acceptance rows

| ID | Acceptance item | Required evidence | Status | Implemented by | Evidence | Gaps / next action |
|---|---|---|---|---|---|---|
| DA-KNOW-01 | WorldFact is separated from KnowledgeEdge. The same fact can have distinct holder states for GM, player_party, PC, NPC, and faction. | V1,V2 | Done | TC-KNOW-FOUND-01 | `same_fact_roundtrips_distinct_holder_states_across_holders` proves one `fact_id` persists distinct holder states across gm, player_party, system, npc_a, and npc_b; existing gate tests keep pc/faction fail-closed until enabled. Lead DB validation `live_knowledge_edges` 11/11 plus model knowledge tests passed. | PC/faction durable holder enablement remains out of scope; current design requires fail-closed behavior. |
| DA-KNOW-02 | PlayerKnowledgeView excludes facts not known/perceived/suspected by player_party, even if GMTruthView knows them. | V1,V3,V4 | Done | TC-VS-KNOW-01 | `JRNY-HOME-01-athena-knowledge-reveal` T1 PASS (`require_player_unknown` + no leak); DB live `player_projection_hides_unknown_fact`; runtime projection tests. | Live cassette promotion pending Milestone 5 only. |
| DA-KNOW-03 | NpcKnowledgeView/NpcMindView is holder-specific: one NPC may know_true, another may believe_false, player may remain unknown. | V1,V2,V4 | Done | TC-KNOW-FOUND-01 | `two_npc_mind_views_diverge_on_same_fact` and DB-backed `two_npcs_diverge_on_same_fact_player_unknown` prove NPC A knows_true, NPC B believes_false, and player_party remains unknown for the same fact; negative controls prove player reveal does not enter NPC minds and NPC-held facts do not enter player projection. Lead validation `npc_mind_view` 6/6, runtime projection 6/6, runtime NPC mind 4/4, and `live_npc_mind_view` 4/4 passed. | None for foundation acceptance; provider-backed live cassette promotion remains Milestone 5 only. |
| DA-KNOW-04 | ContextSurfaced does not grant player knowledge. PlayerExposed reveals entity presence only. PlayerLearnedFact grants fact knowledge only to player_party. NpcLearnedFact grants fact knowledge only to target NPC. | V1,V2,V4 | Done | TC-VS-KNOW-01 | Knowledge checkpoint distinguishes surfaced entities from `player_known_fact_ids`; success T3 records `reveal_committed_fact_ids`; failure branch keeps the fact unknown; DB live holder-specific knowledge tests include player_party-only reveal and NPC holder divergence. | Continue using this as prerequisite evidence for NPC-specific vertical work. |
| DA-KNOW-05 | Reveal is a first-class runtime capability/event path: clue discovery, handout read, NPC disclosure, check interpretation, and GM explicit reveal can update KnowledgeEdge. | V1,V3,V4 | Done | TC-VS-KNOW-01 | Homecoming success T3 shows `domain_events +1` and `knowledge_edges +1`; DB live `reveal_fact_tool_writes_fact_revealed_ledger`; replay/deterministic journey proves revealed fact becomes player-known. | Live cassette promotion pending Milestone 5 only. |
| DA-SPOIL-01 | NoSpoiler is not prompt-only: it filters context before prompt, adds prompt policy, and verifies output after LLM. | V1,V3,V4 | Done | TC-VS-KNOW-01 | `no_spoiler_production_sources` covers context filtering, prompt policy, and post-stream verifier; harness knowledge checkpoint reuses production `scan_player_visible_leak` for player-visible output. | Expand with later non-Homecoming fixtures if desired. |
| DA-SPOIL-02 | Player-unknown facts do not enter player-facing prompt, SSE output, or player memory summary. | V3,V4,V5 | Done | TC-VS-KNOW-01, TC-VS-MEM-01 | Homecoming before-reveal player-visible output and projection PASS; NoSpoiler context-filter tests protect prompt input; HOME-03 memory checkpoint reuses production `scan_player_visible_leak` against `memory_snapshots.summary_markdown` / fixture summaries, and the summary-leak unit test fails closed until the player legitimately knows the fact. | Provider-backed cassette promotion pending Milestone 5 only. |
| DA-SPOIL-03 | VerifierPrivateView may inspect secrets to catch leaks, but those secrets do not enter model-visible prompt. | V1,V4 | Done | TC-PIPE-03 | GM `flight_recorder_provenance::secret_reaches_verifier_but_not_model_visible_prompt` proves `harvest_module_secret_terms` supplies verifier-private secrets to AfterLlmStream, the unknown spoiler block is dropped from model-visible context by ContextFilter, and the finding/trace never echo the secret term; HOME-04 failbranch fails closed as `SECRET_IN_TRACE`. | Provider-backed live cassette promotion pending Milestone 5 only. |
| DA-SPOIL-04 | Once a fact is revealed to the player_party, it becomes eligible for player prompt and narration without being granted to all NPCs. | V1,V3,V4 | Done | TC-VS-KNOW-01 | Homecoming T3/T4 narration may mention Athena only after `player_known_fact_ids` contains the hidden fact; DB live `player_reveal_updates_player_party_only` verifies reveal does not grant all holders. | NPC withholding/use still covered by TC-VS-NPC-01. |
| DA-NPC-01 | NpcProfile stores static persona: traits, values, drives, fears, goals, secrets, speech_style, behavioral_boundaries, source_refs. | V1,V2 | Done | TC-VS-NPC-01 | HOME-02 reach turn requires `profile_present`; lead DB live `live_npc_profiles` 4/4 and model `npc_profile` tests prove profile roundtrip/safe view/source-backed persona fields. | Full live journey recording pending provider key only. |
| DA-NPC-02 | NpcRelationship stores dynamic relation vectors: trust, respect, fear, affection, suspicion, hostility, debt, leverage, stance, interaction_desire, talkativeness. | V1,V2 | Done | TC-VS-NPC-01 | HOME-02 relationship_action turn requires evidence-backed delta; revision1 requires `observed_this_turn`; lead DB live `live_npc_relationships` 5/5 and model `npc_relationship` tests prove bounded deltas/persistence. | Committed-memory replay/idempotency is now covered by TC-VS-MEM-01. |
| DA-NPC-03 | NpcMindView combines NpcProfile, NpcRelationship, and holder-specific KnowledgeEdge. NPC speech/action cannot use unknown facts. | V1,V3,V4 | Done | TC-VS-NPC-01 | HOME-02 guards Athena as NPC-unknown and fails closed through production `scan_player_visible_leak`; `npc_social_checkpoint_tests` include `NPC_ASSERTED_UNKNOWN_FACT`; lead DB live `live_npc_mind_view` 3/3 and runtime/model NPC mind tests prove holder-specific projection. | Live provider journey still blocked without key. |
| DA-NPC-04 | NpcBehaviorPlan is generated before NPC dialogue/action and includes stance, interaction_desire, willingness_to_help/lie/reveal, risk_tolerance, facts_can_reveal, facts_will_withhold, and speech guidance. | V1,V3,V4 | Done | TC-VS-NPC-01 | HOME-02 baseline requires `behavior_plan_present`; GM `npc_behavior_prompt` 5/5 proves active NPC plan enters prompt and excludes GM-only/unknown facts; model `npc_behavior` tests cover source ids, persona boundaries, emotion/goal derivation. | Live executor uses profile presence as a single-process proxy; provider-backed cassette promotion remains pending. |
| DA-NPC-05 | NPC may know a fact but withhold it based on relationship/persona/secret policy; narration may show evasion without revealing hidden truth. | V1,V3,V4,V5 | Done | TC-VS-NPC-01 | HOME-02 baseline withholds `homecoming.fixer.safe_shutdown_sequence` without leaking; revision1 adds `WITHHELD_FACT_NOT_NPC_KNOWN`; changed-response turn permits disclosure only after player-known; leakbranch fails closed as `WITHHELD_SECRET_LEAKED`. | None for deterministic/replay acceptance; real live cassette still needs provider key. |
| DA-MEM-01 | Memory extraction runs from committed turns only and emits proposals, not direct commits. | V1,V2,V4 | Done | TC-VS-MEM-01 | HOME-03 commit turn requires `proposal_present` + `from_committed_turn` and forbids `direct_commit_without_proposal`; failbranch turn 1 fails closed as `EXTRACTION_NOT_FROM_COMMITTED`; runtime/model/DB memory proposal tests prove proposals-only commit path. | Full provider-backed live journey pending key only. |
| DA-MEM-02 | Relationship extraction is triggered by social interaction, not only by new surfaced entity. | V1,V3,V4 | Done | TC-VS-NPC-01 | HOME-02 player help/social action yields an evidence-backed relationship delta; revision1 requires the delta to be observed on the current turn; runtime `relationship_extraction` tests include active NPC social interaction and evidence refs. | Committed-memory extraction/idempotency is now covered by TC-VS-MEM-01. |
| DA-MEM-03 | Extracted facts/knowledge/relationship deltas carry source_event_id/evidence_event_ids and are idempotent. | V1,V2,V4 | Done | TC-VS-KNOW-01, TC-VS-NPC-01, TC-VS-MEM-01 | Knowledge reveal and relationship delta carry durable evidence ids in HOME-01/HOME-02; HOME-03 requires `proposal_evidence_event_ids`, fails closed as `EVIDENCE_MISSING`, and proves committed-memory idempotency through `memory_facts.fact_id` uniqueness plus `memory_proposal_commit` / DB memory proposal tests. | Full provider-backed live journey pending key only. |
| DA-PIPE-01 | execute_turn loads GMTruthView, PlayerKnowledgeView, and active NpcMindViews before context policy plugins run. | V2,V3,V4 | Done | TC-VS-KNOW-01, TC-VS-NPC-01, TC-PIPE-03 | Production `apply_context_assembly_plugins` now records `view_load` provenance for `gm_context`, `npc_mind_views`, and `player_knowledge_view` before context-policy plugin contributions; GM `flight_recorder_provenance::view_loads_recorded_before_plugin_contributions` and HOME-04 deterministic/replay evidence prove ordering, and failbranch fails closed as `VIEW_ORDER_VIOLATION`. | Provider-backed live cassette promotion pending Milestone 5 only. |
| DA-PIPE-02 | Prompt assembly has viewer/speaker-specific projections: GM adjudication, player narration, NPC speech/action, verifier private. | V1,V3,V4 | Done | TC-VS-KNOW-01, TC-VS-NPC-01 | GM/player/verifier projection pieces covered by runtime knowledge tests, NoSpoiler production-source tests, and Homecoming knowledge journey; NPC speech/action projection covered by HOME-02, GM `npc_behavior_prompt`, production leak verifier reuse, and NPC mind/profile/relationship tests. | Extend with provider-backed live cassette when key is available. |
| DA-PIPE-03 | Flight recorder / trace records knowledge view, plugin contributions, filtered context, verifier findings, and ledger-impacting events. | V2,V3 | Done | TC-VS-MEM-01, TC-PIPE-03 | HOME-03 proves memory trace continuity; TC-PIPE-03 consumes the production `TurnTrace.plugin_contributions` type plus same-turn `domain_events` kinds through `FlightRecorderCheckpoint`, requiring `view_load`, `context_filter`, `verifier_finding`, ordered view loads, ledger link, and no secret text in trace summaries. HOME-04 deterministic/replay PASS; failbranch fails closed on view order and secret-in-trace; GM provenance tests cover production contribution categories. | Provider-backed live cassette promotion pending Milestone 5 only. |
